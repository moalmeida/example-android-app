#!groovy
pipeline {
  agent any
  tools { nodejs 'java8' }
  stages {
      stage('Checkout') {
        steps {
          checkout scm
        }
      }
      stage('Dependencies') {
        steps {
          sh 'echo ""'
        }
      }
      stage('Code Scan') {
        steps {
          sh 'echo ""'
        }
      }
      stage('Unit Test') {
        steps {
          // env.NODE_ENV = "test"
          sh 'echo ""'
        }
      }
      stage('Integration Test') {
        steps {
          // env.NODE_ENV = "test"
          sh 'echo ""'
        }
      }
      stage('Build') {
        steps {
          sh 'echo ""'
        }
      }
      stage('Publish') {
        steps {
          sh 'echo ""'
        }
      }
     stage('Deploy') {
       steps {
         sh 'echo ""'
       }
     }
  }
}
